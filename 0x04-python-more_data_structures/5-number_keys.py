#!/usr/bin/python3
def number_keys(theDictionary):
    # Use the len() function to get the number of keys in the dictionary
    num_keys = len(theDictionary.keys())
    # Return the number of keys
    return num_keys
