#!/bin/bash
[ -z "$PYFILE" ] && { echo "Error: PYFILE environment variable is not set."; exit 1; } ; python3 "$PYFILE" # Check if the PYFILE environment variable is not set or is an empty string // Run the Python script
